;import { SongBar } from './SongBar'

const RelatedSongs = ({
  data,isPlaying,active,handlePauseClick={handlePauseClick},handlePlayClick={handlePlayClick}
}) => (
  <div>Loader</div>
);

export default RelatedSongs;

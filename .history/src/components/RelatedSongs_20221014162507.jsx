;import { SongBar } from './SongBar'

const RelatedSongs = ({
  data,isPlaying,active,handlePauseClick,handlePlayClick
}) => (
  <div className='flex flex-col'>
    
  </div>
);

export default RelatedSongs;

;import { SongBar } from './SongBar'

const RelatedSongs = ({
  data={data},isPlaying={isPlaying},active={activeSong},handlePauseClick={handlePauseClick},handlePlayClick={handlePlayClick}
}) => (
  <div>Loader</div>
);

export default RelatedSongs;

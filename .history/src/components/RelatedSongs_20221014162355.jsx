;import { SongBar } from './SongBar'

const RelatedSongs = ({
  data,isPlaying={isPlaying},active={activeSong},handlePauseClick={handlePauseClick},handlePlayClick={handlePlayClick}
}) => (
  <div>Loader</div>
);

export default RelatedSongs;
